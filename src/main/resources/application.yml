spring:
  h2:
    console:
      enabled: true
  jpa:
    defer-datasource-initialization : true
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    generate-ddl: false
    hibernate:
      ddl-auto: create-drop
  datasource:
    url: jdbc:mysql://182.162.143.216:3306/book_manager
    username: sschina
    password: Andy4240!@
    initialization-mode: always
    sql-script-encoding: UTF-8

server:
  port: 8070
logging:
  level:
    root: trace

